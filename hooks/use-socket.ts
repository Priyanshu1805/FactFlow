import { useContext } from "react"
// Re-exporting useSocket from the provider file for easier imports if needed, 
// but usually we can just import it directly from the provider.
export { useSocket } from "@/components/providers/socket-provider"
